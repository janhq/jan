{
  "name": "jan-electron",
  "version": "1.0.0",
  "description": "Hello World!",
  "main": "./build/main.js",
  "author": "Jan",
  "license": "MIT",
  "build": {
    "appId": "jan.ai.app",
    "productName": "Jan Desktop App",
    "files": [
      "renderer/**/*",
      "build/*.{js,map}",
      "build/core/plugin-manager/**/*"
    ],
    "extends": null,
    "mac": {
      "type": "distribution"
    }
  },
  "scripts": {
    "dev": "tsc -p . && electron .",
    "build": "tsc -p . && electron-builder build --publish never -mwl"
  },
  "dependencies": {
    "electron-is-dev": "^2.0.0",
    "node-llama-cpp": "^2.4.1",
    "pluggable-electron": "^0.6.0",
    "request": "^2.88.2",
    "request-progress": "^3.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.1",
    "electron": "26.2.1",
    "electron-builder": "^24.6.4",
    "wait-on": "^7.0.1"
  },
  "installConfig": {
    "hoistingLimits": "workspaces"
  }
}
