[package]
name        = "code-exec-tool"
version     = "0.1.0"
edition     = "2021"
description = "code.exec skill for Jan — delegates to the host JS/Python runtime (WASM tool)"
publish     = false

[lib]
crate-type = ["cdylib"]

[dependencies]
serde_json = "1"
