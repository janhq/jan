{
  "description": "Fetch any URL via HTTP GET and return the response body (truncated to 32 KB). Use for REST APIs, JSON endpoints, or raw web pages.",
  "parameters": {
    "type": "object",
    "properties": {
      "url": { "type": "string", "description": "Full URL to fetch (https://...)" },
      "method": { "type": "string", "enum": ["GET"], "default": "GET", "description": "HTTP method (GET only)" }
    },
    "required": ["url"],
    "additionalProperties": false
  }
}
