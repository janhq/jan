{
  "description": "Search the web. Tries Brave Search API first (set BRAVE_API_KEY env var), then DuckDuckGo HTML, then DuckDuckGo Instant Answer as fallback. Returns numbered result snippets with titles and URLs.",
  "parameters": {
    "type": "object",
    "properties": {
      "query": { "type": "string", "description": "Search query" },
      "max_results": { "type": "integer", "description": "Maximum number of results to return (default: 5)", "default": 5 }
    },
    "required": ["query"],
    "additionalProperties": false
  }
}
