{
  "description": "Execute code in an isolated sandbox (microVM or WASM fallback). Languages: 'javascript' (full Node.js), 'python', 'bash'. Use 'workspace' for persistent state across calls.",
  "parameters": {
    "type": "object",
    "properties": {
      "language": { "type": "string", "enum": ["javascript", "python", "bash"], "description": "Runtime to use" },
      "code": { "type": "string", "description": "Code to execute" },
      "workspace": { "type": "string", "description": "Persistent workspace ID. Omit for ephemeral one-shot execution." }
    },
    "required": ["language", "code"],
    "additionalProperties": false
  }
}
