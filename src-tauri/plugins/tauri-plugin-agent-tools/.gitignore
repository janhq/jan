/.vs
.DS_Store
package-lock.json
yarn.lock

/.tauri
/target
Cargo.lock
node_modules/

dist-js
dist

# Regenerated by tauri_plugin::Builder on every build; committing it only
# creates drift (see the stale probe_backend_load entries in llamacpp).
permissions/autogenerated/
