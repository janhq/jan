[package]
name        = "http-fetch-tool"
version     = "0.1.0"
edition     = "2021"
description = "HTTP fetch skill for Jan (WASM tool)"
publish     = false

[lib]
crate-type = ["cdylib"]

[dependencies]
serde_json = "1"
