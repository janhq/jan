# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dist-web
*.local

# Bundled seed catalog snapshots — fetched at build time by
# `scripts/fetch-seed-catalog.mjs`. Tracking them in git would force a
# new commit on every release just to refresh a derived artefact.
public/seed-catalog.json.gz
public/seed-catalog.idx.json.gz

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
